# Changelog — 2026-08-07

## fix: ensure stylesheet load triggers layout remeasure in slider certificaciones new

### Descripción
Se corrigió un problema de sincronización asíncrona al cargar CSS dinámicamente en Webflow para `widgets/slider certificaciones new/slider-certificaciones.js`:

1. **Recálculo tras Carga de Estilos**:
   - Se añadió un listener al objeto `<link rel="stylesheet">` en `injectStylesheet()` para re-ejecutar `initAll()` una vez que el CSS se descargue por completo.
   - Se aplicó `requestAnimationFrame` en `loadMarkup()` para garantizar que la medición del paso y ancho de tarjetas (`measure()`) se realice sobre el DOM ya renderizado con los estilos responsivos del widget aplicados.

### Archivos modificados
- **`widgets/slider certificaciones new/slider-certificaciones.js`**
- **`slider certificaciones new/slider-certificaciones.js`**

### Commit
- `0d1f4e7` - `fix: ensure stylesheet load triggers layout remeasure in slider certificaciones new`

---

## style: optimize mobile layout and image containment for slider certificaciones new

### Descripción
Se reforzó la adaptabilidad y contención de recursos en dispositivos móviles para `widgets/slider certificaciones new/slider-certificaciones.css`:

1. **Contención de Imágenes en Móvil**:
   - Se forzó `max-width: 75% !important`, `max-height: 75% !important` y `object-fit: contain !important` en `.gpk-cert-media img` dentro de `@media (max-width: 639px)` para evitar recortes, ampliaciones desproporcionadas o interferencia con estilos globales de Webflow.
2. **Dimensionamiento de Tarjetas e Inserción Móvil**:
   - Se aplicó `width: 100%`, `max-width: 100%`, `box-sizing: border-box` y `overflow: hidden` en el viewport, track y elementos `li` para asegurar que las tarjetas encajen perfectamente en pantallas móviles (smartphones) sin desbordamiento horizontal.

### Archivos modificados
- **`widgets/slider certificaciones new/slider-certificaciones.css`**
- **`slider certificaciones new/slider-certificaciones.css`**

### Commit
- `c8242ac` - `style: optimize mobile layout and image containment for slider certificaciones new`

---

## fix: update silver ISO image asset and sync root slider certificaciones css

### Descripción
Se corrigió la imagen ISO y se sincronizaron todos los estilos en `slider certificaciones new`:

1. **Reemplazo de Imagen ISO**:
   - Se actualizó el archivo de imagen `widgets/slider certificaciones new/Images/iso.png` con la versión plata correcta (`iso.png` de 41 KB) en lugar de la versión previa a color.
2. **Sincronización Total de Estilos**:
   - Se aseguraron los estilos de encabezado (`max-width: 13ch`), alineación del tagline y padding de `20px` en todos los archivos CSS tanto en el directorio `widgets/` como en la carpeta raíz.

### Archivos modificados
- **`widgets/slider certificaciones new/Images/iso.png`**
- **`slider certificaciones new/slider-certificaciones.css`**

### Commit
- `5056060` - `fix: update silver ISO image asset and sync root slider certificaciones css`

---

## fix: resolve base URL dynamically from scriptSource to use jypesa-widgets domain in production

### Descripción
Se corrigió la resolución de `baseURL` en `widgets/slider certificaciones new/slider-certificaciones.js`:

1. **Resolución Dinámica de Dominio y Recursos**:
   - Se eliminó la restricción `protocol === "file:"` que provocaba que al incrustar el script en Webflow vía HTTPS se ignorara la URL del script y se forzara el dominio anterior `grupak-widgets.vercel.app`.
   - Ahora detecta dinámicamente la URL real del script (`https://jypesa-widgets.vercel.app/widgets/slider%20certificaciones%20new/slider-certificaciones.js`) para descargar los estilos CSS actualizados, el HTML y los nuevos recursos de imagen (`Images/iso.png`).

### Archivos modificados
- **`widgets/slider certificaciones new/slider-certificaciones.js`**
- **`slider certificaciones new/slider-certificaciones.js`**

### Commit
- `1351796` - `fix: resolve base URL dynamically from scriptSource to use jypesa-widgets domain in production`

---

## style: adjust tagline position and title wrapping for last slide in slider certificaciones new

### Descripción
Se ajustó la alineación y el ancho máximo del título en la cabecera de las tarjetas del widget `widgets/slider certificaciones new/slider-certificaciones.css`:

1. **Ajuste del Tagline y Títulos Largos en Desktop**:
   - Se configuró `max-width: 13ch` en `.gpk-cert-header h2` para permitir división limpia en 2 líneas de títulos largos ("Cumplimiento Regulatorio").
   - Se ajustó el layout grid a `grid-template-columns: minmax(0, auto) 1px minmax(0, 1fr)` para que el texto "Calidad que genera confianza" quede desplazado más a la izquierda, junto al divisor `1px`, evitando desbordamientos en desktop.

### Archivos modificados
- **`widgets/slider certificaciones new/slider-certificaciones.css`**

### Commit
- `fa95315` - `style: adjust tagline position and title wrapping for last slide in slider certificaciones new`

---

## style: add 20px track desktop padding and mobile responsiveness for slider certificaciones new

### Descripción
Se realizaron mejoras visuales y de adaptabilidad responsiva en `widgets/slider certificaciones new/slider-certificaciones.css`:

1. **Padding en Desktop para `.gpk-cert-widget .gpk-cert-track`**:
   - Se asignó `padding: 20px` a `.gpk-cert-widget .gpk-cert-track` para evitar cortes de sombreado (`box-shadow`) en las tarjetas.
2. **Adaptación Móvil Full Responsiva**:
   - Se ajustó el padding del widget en dispositivos móviles (`@media (max-width: 639px)`).
   - Se optimizaron tamaños de fuente, espacio interno (`padding` del body de las cards y header) y espacio de botones de navegación para evitar superposiciones y garantizar legibilidad perfecta en pantallas pequeñas.

### Archivos modificados
- **`widgets/slider certificaciones new/slider-certificaciones.css`**

### Commits
- `58e84cf` - `style: add 20px track desktop padding and mobile responsiveness for slider certificaciones new`

---

## style: add 20px desktop padding to gpk-track in slider-certificaciones

### Descripción
Se aplicó un padding de 20px en la vista desktop (`@media (min-width: 769px)`) para `.gpk-cert-widget .gpk-cert-track` en `widgets/slider-certificaciones/slider-certificaciones.js`.

1. **Ajuste de Padding en Desktop**:
   - Se añadió `.gpk-cert-widget .gpk-cert-track` y `.gpk-cert-track` con `padding: 20px !important` en vista desktop.
2. **Actualización de clases en el HTML**:
   - Se agregaron las clases `.gpk-cert-widget` y `.gpk-cert-track` al HTML generado por el widget y en la consulta de selector del contenedor para máxima compatibilidad con las clases Webflow.

### Archivos modificados
- **`widgets/slider-certificaciones/slider-certificaciones.js`**

### Commit
- `2a78f44` - `style: add 20px desktop padding to gpk-cert-track in slider-certificaciones`
