# Reglas de Comportamiento (Workspace)

- **No abrir el navegador (ESTRICTO)**: NUNCA uses la herramienta `browser_subagent` u otras herramientas para interactuar o abrir el navegador web de forma automática a menos que el usuario lo indique de manera explícita en su solicitud.
